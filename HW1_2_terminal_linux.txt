find . -name lesson_2.txt  ищем путь к файлу

tail -f -n5 2.txt просмотр последних пяти строк файла в реальном вермени

tail -f -n5 2.txt | grep abc --line-buffered вывод строк из последних 5 строк строк где встречается abc в реальном времени


tail -f -n5 2.txt | grep --line-buffered -i  abc >> lesson_1.txt запись строк в файл из последних 5 строк строк где встречается abc в реальном времени




